const drawSideBar = () => {
    const container = document.getElementById("sidebar");
        container.style.background = '#000';
        container.style.border = '1px';
        container.style.width = `${tile_size * 5}px`;
        container.style.height = `${tile_size * 5}px`;
};